<h1>Editing person</h1>

<%=
  render(
    partial: "form",
    locals: {
      person: @person,
      path: person_path(@person),
      scope: :person
    }
  )
%>

<br>

<div>
  <%= link_to "Show", person_path(@person) %> |
  <%= link_to "Back to address", county_path(@person.address) %>
</div>